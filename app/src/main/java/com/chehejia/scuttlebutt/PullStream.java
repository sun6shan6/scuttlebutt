package com.chehejia.scuttlebutt;

public class PullStream {

    void link(Duplex c, Duplex s) {
//        pull(c.source, s.sink);
    }

    void pull(final Duplex.Source sources, Duplex duplex) {
//        sink.read(sources);
        duplex
    }
}
